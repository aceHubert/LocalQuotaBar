## [2026-09-15 18:11 +0800] | 任务：重置卡临近到期显示小时分钟

### 执行上下文

- **Agent ID**：`codex`
- **Base Model**：`GPT-6`
- **Runtime**：`Codex 桌面应用，macOS，SwiftPM`
- **Git User**：`hubert <hubert@lejian.com>`
- **Branch**：`main`

### 用户诉求

> 当天到期不应显示“1天后到期”，应显示小时和分钟。

### 变更概览

**影响范围**：重置卡到期徽标文案。

- 剩余不足 24 小时时显示“2小时15分钟后到期”等文案；不足 1 小时只显示分钟。
- 按分钟向上取整，避免尚未到期却显示零分钟。
- 更远的到期时间保持按天显示，缺少日期和已过期状态保持原语义。

### 设计动机

此前所有未来日期都按天向上取整，几小时后到期也显示为“1天后到期”。细化短时间段可准确表达临近到期程度，也覆盖跨午夜但不足一天的情况。

### 验证结果

- `swift build`、`swift build -c release`、`git diff --check`：通过。
- 直接提取生产格式函数进行 6 项临时检查：2小时15分钟、45分钟、不足一分钟、满一天、已到期和无日期，全部通过。
- 已安装并重启，`cmp` 确认安装二进制与发布版一致。
- 未新增单元测试，未修改额度刷新或提醒投递逻辑。

### 变更统计

- **统计口径**：与本轮修改前快照执行 `git diff --no-index --shortstat` 和 `--numstat`；排除历史记录自身和此前已有改动。
- **变更文件数**：1
- **新增行数**：+9
- **删除行数**：-2

| 文件 | 新增 | 删除 |
| --- | ---: | ---: |
| `Sources/LocalQuotaBar/PanelTheme.swift` | 9 | 2 |

### 修改文件

- `Sources/LocalQuotaBar/PanelTheme.swift`
- 本历史记录。
