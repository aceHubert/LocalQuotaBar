## [2026-09-15 15:01 +0800] | 任务：修复菜单栏点击失效并恢复灵动岛通道

### 执行上下文

- **Agent ID**：`codex`
- **Base Model**：`未知`
- **Runtime**：`Codex 桌面应用，macOS，本机 SwiftPM`
- **Git User**：`hubert <hubert@lejian.com>`
- **Branch**：`main`

### 用户诉求

> 读取指定 ZCode 会话最后几轮，修复修改后菜单栏点击无效、额度数字消失的问题；不得摘除 DynamicNotchKit 通道。

### 变更概览

**影响范围**：菜单面板页脚、提醒投递顺序、SwiftPM 测试目标。

- 将页脚控件加入属性 reminderLine，消除同名局部变量遮蔽。
- 恢复 Touch Bar → DynamicNotchKit → 自画刘海卡片的通道顺序。
- 注册 XCTest 目标，添加首次配置、恢复按钮显隐切换、控制器加载三项回归测试。

### 设计动机

此前初始化将恢复按钮加入局部 stack，configure 却对空的属性 stack 设置按钮可见性，触发 AppKit 的“视图必须属于该 stack”断言。系统日志调用栈明确经过 syncControlStates、showPopover 和菜单栏点击入口。启动时 applyReminderConfiguration 同样进入该路径，发生在额度 store 启动之前，因此可以同时解释占位数字和弹窗打不开。

本次修复直接消除错误引用，不改动已有面板设计。此前会话使用旧崩溃报告归因于 DynamicNotchKit，缺乏与本次断言对应的证据，按用户要求撤销临时摘除。

### 验证结果

- `swift build`、`swift build -c release`：通过。
- `timeout 60 arch -arm64 /usr/bin/swift test`：3 项测试通过，0 失败；恢复通道后再次执行。
- 普通 `swift test` 首次运行因宿主选择 x86_64 XCTest 运行器而无法加载 arm64 测试包，显式指定本机架构后通过。
- macOS 辅助功能实测：修复版菜单栏从占位值恢复为实际额度；实际鼠标点击出现额度面板，Codex/Z.AI 数据可见；齿轮可以进入设置页。
- 修复后的日志检查未出现同类 NSStackView 断言。
- 多屏坐标切换期间，连续关闭重开的自动验证未完成；截图工具未能取得指定区域，未将其记为通过。
- 未覆盖：Touch Bar / 刘海提醒的设备实际投递、失败后缓存保留、刷新频率计时。提醒通道恢复原实现，未修改阈值、冷却或静音算法。

### 变更统计

- **统计口径**：已存在的改动使用本次修改前快照与最终文件执行 `git diff --no-index --shortstat`、`--numstat`；新增测试与 `/dev/null` 对比。排除历史记录自身及此前 UI、图标等未提交改动。
- **变更文件数**：4
- **新增行数**：+90
- **删除行数**：-5

| 文件 | 新增 | 删除 |
| --- | ---: | ---: |
| `Sources/LocalQuotaBar/QuotaPanelViews.swift` | 4 | 1 |
| `Sources/LocalQuotaBar/ReminderCenter.swift` | 4 | 4 |
| `Package.swift` | 5 | 0 |
| `Tests/LocalQuotaBarTests/PanelFooterViewTests.swift` | 77 | 0 |

### 修改文件

- `Sources/LocalQuotaBar/QuotaPanelViews.swift`
- `Sources/LocalQuotaBar/ReminderCenter.swift`
- `Package.swift`
- `Tests/LocalQuotaBarTests/PanelFooterViewTests.swift`
- 本历史记录。
